HTML importent notes:

All HTML elements can have attributes
The href attribute of <a> specifies the URL of the page the link goes to
The src attribute of <img> specifies the path to the image to be displayed
The width and height attributes of <img> provide size information for images
The alt attribute of <img> provides an alternate text for an image
The style attribute is used to add styles to an element, such as color, font, size, and more
The lang attribute of the <html> tag declares the language of the Web page
The title attribute defines some extra information about an element

Tag	Description--
<p>	Defines a paragraph
<hr>	Defines a thematic change in the content
<br>	Inserts a single line break
<pre>	Defines pre-formatted text

Use the style attribute for styling HTML elements
Use background-color for background color
Use color for text colors
Use font-family for text fonts
Use font-size for text sizes
Use text-align for text alignment

<b> - Bold text
<strong> - Important text
<i> - Italic text
<em> - Emphasized text
<mark> - Marked text
<small> - Smaller text
<del> - Deleted text
<ins> - Inserted text
<sub> - Subscript text
<sup> - Superscript text

Tag	Description--
<abbr>	Defines an abbreviation or acronym
<address>	Defines contact information for the author/owner of a document
<bdo>	Defines the text direction
<blockquote>	Defines a section that is quoted from another source
<cite>	Defines the title of a work
<q>	Defines a short inline quotation

Use the HTML style attribute for inline styling
Use the HTML <style> element to define internal CSS
Use the HTML <link> element to refer to an external CSS file
Use the HTML <head> element to store <style> and <link> elements
Use the CSS color property for text colors
Use the CSS font-family property for text fonts
Use the CSS font-size property for text sizes
Use the CSS border property for borders
Use the CSS padding property for space inside the border
Use the CSS margin property for space outside the border

<a href="" target="">
_self	- Opens the link in the same window/tab. (This is the default behavior.) 
W3Schools
+1
_blank	- Opens the link in a new window or tab. 
W3Schools
_parent	- Opens the link in the parent frame (useful when there are frames/iframes). 
W3Schools
_top	- Opens the link in the full body of the window, removing any frames



Form:
<form 
  action="submit_form.php"
  method="post"
  enctype="multipart/form-data"
  target="_blank"
  name="registerForm"
  autocomplete="on"
>
  <label for="name">Name:</label>
  <input type="text" id="name" name="username"><br><br>

  <label for="photo">Upload Photo:</label>
  <input type="file" id="photo" name="photo"><br><br>

  <input type="submit" value="Submit">
</form>

<---Explaintaion--->
1action

👉 কাজ: form-এর data কোথায় (কোন ফাইলে বা URL-এ) পাঠানো হবে সেটা নির্ধারণ করে।
🧩 উদাহরণ:

<form action="LoginServlet">


এখানে form সাবমিট করলে ডেটা LoginServlet ফাইলে যাবে।

2️ method

👉 কাজ: ডেটা কীভাবে পাঠানো হবে সেটা বলে।
দুই ধরনের হয় —

Method		কাজ			বৈশিষ্ট্য
GET	ডেটা URL-এর মধ্যে পাঠায়	দ্রুত কিন্তু নিরাপদ নয় (URL-এ ডেটা দেখা যায়)
POST	ডেটা body-র মধ্যে পাঠায়	নিরাপদ এবং বড় ডেটা পাঠানো যায়

🧩 উদাহরণ:

<form method="post">

3️ enctype

👉 কাজ: ডেটা পাঠানোর আগে সেটাকে কীভাবে encode করা হবে, সেটা নির্ধারণ করে।
বিশেষ করে file upload এর সময় এটা দরকার হয়।

Value						কাজ
application/x-www-form-urlencoded	ডিফল্ট (সাধারণ ডেটা পাঠায়)
multipart/form-data			ফাইল (image, pdf, etc.) পাঠাতে লাগে
text/plain				সাধারণ টেক্সট আকারে পাঠায়

🧩 উদাহরণ:

<form enctype="multipart/form-data">


👉 এটা image/file upload করার সময় অবশ্যই দিতে হয়।

4️ target

👉 কাজ: form সাবমিট করার পর রেজাল্ট কোথায় দেখাবে সেটা বলে।

Value	মানে
_self	একই ট্যাবে দেখায় (default)
_blank	নতুন ট্যাবে খুলে
_parent	parent frame-এ দেখায়
_top	পুরো window-তে দেখায়

🧩 উদাহরণ:

<form target="_blank">


👉 রেজাল্ট নতুন ট্যাবে খুলবে।

5️ name

👉 কাজ: form-কে একটা নাম দেয়, যাতে JavaScript বা অন্য কোডে সহজে চিনতে পারি।
🧩 উদাহরণ:

<form name="registerForm">

6️ autocomplete

👉 কাজ: ব্রাউজার আগের লেখা (যেমন নাম, ইমেইল) মনে রাখবে কি না।

Value	মানে
on	ব্রাউজার আগের ডেটা সাজেস্ট করবে
off	ব্রাউজার কিছুই মনে রাখবে না

🧩 উদাহরণ:

<form autocomplete="on">


---select tag---
<select> = dropdown বানানোর ট্যাগ

<option> = প্রতিটা item বা choice

value = user যা select করবে, সেটা সার্ভারে পাঠানো হবে

selected = শুরুতেই কোনটা select থাকবে

multiple = একসাথে একাধিক নির্বাচন করা যাবে


